Add the lemon logo image file here as `lemon.png` (recommended size ~192x192 or svg) to use as the site logo and social card image.

Place the file at `public/images/lemon.png` and it will be served at `/images/lemon.png`.

You can copy the provided lemon image into this location.
